source_up
dotenv_if_exists .env

# Repo-local secrets live in .env and are loaded if present.

# Optional in .env when using the Ollama provider:
# OLLAMA_SESSION_COOKIE=

# Required in .env for the OTLP sink server.
# Run `just install-service` after changing it.
# OPENROUTER_SINK_TOKEN=
