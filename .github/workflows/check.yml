name: Check
'on':
  push:
    branches:
    - main
  pull_request:
    branches:
    - main
jobs:
  check:
    uses: dzackgarza/opencode-plugins/.github/workflows/python-cli-ci.yml@main
    secrets: inherit
