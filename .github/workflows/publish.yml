name: Publish to PyPI
'on':
  push:
    tags:
    - v*
  workflow_dispatch: {}
jobs:
  publish:
    uses: dzackgarza/opencode-plugins/.github/workflows/python-cli-publish.yml@main
    with:
      package_name: usage-limits
    secrets: inherit
